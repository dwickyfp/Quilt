# Rust
target/
**/*.rs.bk
*.pdb
Cargo.lock.bak

# Tauri build artifacts
apps/desktop/target/
apps/desktop/gen/
# The bundled runner is produced at build time, not committed. Keep the
# directory itself (via .gitkeep) so the bundle.resources glob always
# matches; ignore only the binaries staged into it.
apps/desktop/bin/*
!apps/desktop/bin/.gitkeep

# Node / frontend
node_modules/
dist/
.vite/
*.tsbuildinfo
.pnpm-store/
.npm/
.yarn/

# Editor / OS
# Claude Code local config - keep out of version control
.claude/
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# Logs / temp
*.log
logs/
*.tmp
.env
.env.local
.env.*.local

# Coverage
coverage/
*.profraw

# Embedded engines downloaded at runtime
.quilt/cache/
.quilt/engines/

# Local DuckDB CLI for tests (QUILT_DUCKDB_BIN)
.duckdb-cli/
.duckdb-cli-macos/

# stray sink test outputs
samples/*.parquet
house-price-demo/
